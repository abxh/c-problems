# Histogram

Given an array of numbers and integer `k`, count the number of `0`s, `1`s, ..., `k-1`s in the array and store the result in the `k` bins.

The challenge lies in solving this problem with atomics.
